Basic PIC's but with Usage clauses such as DISPLAY, COMP and COMP-3.
With the above, talk about how memory is stored in each of these, ie; (binary, 1 per byte, 2 per byte, where sign is stored)
Using sections or paragraphs. Sections can be used with Declaratives, paragraphs can not.
Certain COBOL features can only be used with sectioned code (e.g. declaratives, internal sorts)
Speaking of SORT, we can go through setting up input and output files, plus SORT files. (FD, SD)
Lining up to's and thru's as well as PIC, VALUE, etc.
